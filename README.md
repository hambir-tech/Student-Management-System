# Student-Management-System
User logs in via a HashMap&lt;String, String> (username → password). Upon successful login, a unified class presents a menu for student management using an ArrayList&lt;Student>, and supports modular methods for add, delete, modify, search, sort and report operations.
